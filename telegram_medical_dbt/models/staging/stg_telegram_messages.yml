version: 2

models:
  - name: fct_messages
    description: "Fact table of Telegram messages"
    columns:
      - name: message_id
        tests:
          - unique
          - not_null
      - name: sent_at
        tests:
          - not_null
